/**
 * mode-cycle.ts — RETIRED
 *
 * Ctrl+Shift+M cycling is now handled directly by pi-modes (patched in
 * npm/node_modules/pi-modes/index.ts). This file is kept as a no-op stub
 * to avoid "missing extension" warnings.
 */
export default function () {}
